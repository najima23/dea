import { createAction, props } from '@ngrx/store';

export const getGrammar = createAction('[Grammar] test');

// export const getGrammar = createAction('[Grammar] test', props<{ username: string; password: string }>());
